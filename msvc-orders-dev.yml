server:
  port: 8001

spring:
  datasource:
    url: jdbc:postgresql://db.yhnqvpahxejfqltzzjxp.supabase.co:5432/postgres
    username: postgres
    password: B4$3d3d4t0$
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: order_schema
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
